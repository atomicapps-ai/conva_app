# Moved to conva_core

This cross-platform (desktop / web / mobile) convention now lives in the knowledge repo (core is the single source of truth for all design docs):

**→ `conva_core/docs/technical/multiplatform.md`**

See also `conva_core/docs/technical/CONVA_ARCHITECTURE.md` for the shared-layer model it serves.
