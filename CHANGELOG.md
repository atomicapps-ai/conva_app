# Changelog

All notable changes to **conva** are documented here. Entries follow
[Conventional Commits](https://www.conventionalcommits.org) grouping and are
meant to be regenerated by [git-cliff](https://git-cliff.org) (`cliff.toml`,
`git-cliff -o CHANGELOG.md`) as part of the release checklist
([`docs/releasing.md`](docs/releasing.md)) — don't hand-edit a past release's
entries, only the `[Unreleased]` section between releases.

## [Unreleased]

### Features

- Point the in-app updater and the release pipeline at a separate public
  releases repo (`atomicapps-ai/conva_releases`), so installers, the
  updater's `latest.json`, and public release notes no longer require the
  private `conva_app` repo to be reachable unauthenticated.

### Documentation

- Add `docs/development.md`, `docs/releasing.md`, `docs/ai-workflow.md`,
  `AGENTS.md`, and a PR template describing the release/dev workflow and
  AI-assistant guardrails (no secrets, no direct releases, small branches,
  tests required).

## [0.1.1] — 2026-07-31

### Features

- Live dual-column transcription of both sides of a call, captured from
  system audio — no bot joins the meeting.
- Ally answers grounded in your document vault, with a citation on every
  reply.
- On-device by default: audio, transcripts, and your library stay on your
  machine.
- Hybrid retrieval (BM25 + fastembed/ONNX embeddings, reciprocal-rank
  fusion), degrading gracefully to BM25-only until the embedding model is
  ready.
- Commitment & entity tracker: a per-session background pass extracts
  who/what/due into a collapsible right rail.
- Sessions persist to disk and re-open; the Question Radar surfaces
  library-grounded answers to inbound questions with zero LLM cost; the
  shown transcript exports to Markdown.
- Every build shows its version and commit (status bar + Settings → About),
  so it's always clear exactly what's running.

<!-- generated by git-cliff -->
